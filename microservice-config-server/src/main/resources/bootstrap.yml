encrypt:
  key: foo


spring:
  application:
    name: microservice-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/aijiangbin/springcloud-config
server:
  port: 8080